{
  "name": "faultspan-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply faultspan --local",
    "db:migrate:remote": "wrangler d1 migrations apply faultspan --remote"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "viem": "^2.28.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250725.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
