# apps/web (Next.js) — see HANDOVER.md for full setup steps
NEXT_PUBLIC_GENLAYER_RPC_URL=https://studio.genlayer.com/api
NEXT_PUBLIC_GENLAYER_CHAIN_ID=61999
NEXT_PUBLIC_FAULTSPAN_CONTRACT_ADDRESS=
NEXT_PUBLIC_PLATFORM_API_URL=https://faultspan-worker.<your-cloudflare-subdomain>.workers.dev

# services/worker (Cloudflare Worker) — configured via `wrangler secret put`, not this file.
# Listed here for reference only:
#   ALLOWED_ORIGINS  -> set in services/worker/wrangler.jsonc "vars"
#   PINATA_JWT       -> wrangler secret put PINATA_JWT
#   DB (D1), KV      -> bound in services/worker/wrangler.jsonc
